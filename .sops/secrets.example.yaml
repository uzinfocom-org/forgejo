# Plaintext template — copy to secrets.yaml and encrypt:
#   sops --encrypt .sops/secrets.example.yaml > .sops/secrets.yaml
# Or create from scratch (opens $EDITOR, saves encrypted):
#   sops .sops/secrets.yaml
forgejo:
  url: https://your-forgejo.example.com
  token: your-api-token-here

test:
  owner: your-username
  repo: existing-test-repo
  org: optional-org-name
